import React from "react";
import ReactDOM from "react-dom";

// functional component
import App from "./App";
import "./global.css";

// render the App component to the DOM from the index.html container with id, root
ReactDOM.render(<App />, document.getElementById("root"));
